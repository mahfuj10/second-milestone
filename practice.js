
/* document.getElementById('submit').addEventListener('click',function(){
    const userComment = document.getElementById('text-area');
    const p = document.createElement('p');
    p.innerText = userComment.value;
    const parent = document.getElementById('section');
    parent.appendChild(p);
    userComment.value = '';
}) */

/* document.getElementById('submit').addEventListener('click', function(){
    const userCommment = document.getElementById('text-area');
    const p = document.createElement('p');
    p.innerText = userCommment.value;
    const parent = document.getElementById('section');
    parent.appendChild(p);
    userCommment.value = ''
})
 */
/* document.getElementById('submit').addEventListener('click',function(){

const textField = document.getElementById('text-area');
const userComment = textField.value;
const p = document.createElement('p');
p.innerText = userComment;
const parent = document.getElementById('section');
parent.appendChild(p);
textField.value = '';

})
*/

/* let x = document.getElementById('pass');
x.addEventListener('keyup',function(){

if(x.value == 'delete'){
    document.getElementById('delete').removeAttribute('disabled');
}else{
    document.getElementById('delete').setAttribute('disabled',true)
}
}); */